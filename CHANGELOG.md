# Changelog

All notable changes to `absolutely-nothing` will be documented in this file.

---

## [1.0.0] — 2024-01-01

### Added
- `absolutelyNothing()` — core functionality
- `doNothing()` — alias for users who prefer clarity
- `makeNothing()` — for the creatively inclined
- `createVoid()` — enterprise naming convention compliant
- `noop()` — for veterans
- `empty()` — for minimalists
- `getNothing()` — returns `undefined` with confidence
- `stillNothing()` — consistency across multiple calls verified
- `awaitNothing()` — async support for modern nothing
- `fixYourCode()` — does not fix your code
- `improvePerformance()` — does not improve performance
- `solveAllProblems()` — does not solve all problems
- `enterpriseNothing()` — exactly the same as `noop()`, but billable
- `productionReadyNothing()` — zero known bugs (zero known features)
- `scaleNothing()` — scales linearly with zero load
- TypeScript definitions for all of the above
- MIT License
- This CHANGELOG

### Summary
Absolutely nothing works as expected.

---

## [1.0.1] — 2024-01-08

### Fixed
- Fixed an issue where something almost happened.

### Changed
- Nothing.

---

## [1.0.2] — 2024-02-14

### Security
- Patched a theoretical vulnerability where calling `solveAllProblems()` could create false hope. Behavior unchanged. Hope removed.

---

## [Unreleased]

### Planned
- `doNothingFaster()` — performance improvements (estimated: saves 0ms)
- `nothingV2()` — nothing, but rewritten in Rust
- `enterpriseNothingPlus()` — requires a vendor agreement to call
- GUI for doing nothing (roadmap item #1, forever)
